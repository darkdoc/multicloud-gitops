{{- if .Values.vpManageProxyClusterCa.pushAPIserverCAbundle  }}
---
apiVersion: external-secrets.io/v1alpha1
kind: PushSecret
metadata:
  name: apiserver-ca-ps
  namespace: openshift-config-managed
spec:
  updatePolicy: Replace
  deletionPolicy: Delete
  refreshInterval: 15s
  secretStoreRefs:
    - kind: ClusterSecretStore
      name: vault-backend
  selector:
    secret:
      name: kube-apiserver-server-ca-intermediary
  data:
    - conversionStrategy: None
      match:
        secretKey: ca-bundle.crt
        remoteRef:
          remoteKey: pushsecrets/{{ .Values.global.clusterDomain }}-kube-apiserver-ca-{{ .Values.vpManageProxyClusterCa.pushSecretPostfix }}
          property: cert
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: kube-apiserver-server-ca-intermediary
  namespace: openshift-config-managed
spec:
  refreshInterval: 15s
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault-backend
  target:
    name: kube-apiserver-server-ca-intermediary
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      mergePolicy: Merge
      engineVersion: v2
      templateFrom:
      - target: Data
        configMap:
          # name of the configmap to pull in
          name: kube-apiserver-server-ca
          # here you define the keys that should be used as template
          items:
          - key: ca-bundle.crt
            templateAs: Values
  data:
  # TODO FIX this needs to point to secret in vault
  - secretKey: asd
    remoteRef:
      key: "global/config-demo"
      property: secret
{{- end }}
